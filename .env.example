# Google Apps Script web-app URL that receives form submissions.
# Deploy > New deployment > Web app  (Execute as: Me / Who has access: Anyone)
# Copy the URL ending in /exec.
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/AKfycb.../exec

# Optional. Must match CONFIG.SHARED_SECRET in google-apps-script/Code.gs.
# Leave unset if you left SHARED_SECRET empty in the script.
GOOGLE_SCRIPT_SECRET=

# PostHog analytics — visit counts, per-page session duration, session
# recordings/heatmaps for friction, and the inquiry_submitted funnel event
# (lib/analytics.ts). Get these from your PostHog project settings. Analytics
# stays fully disabled (no network calls) when NEXT_PUBLIC_POSTHOG_KEY is unset.
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
